const CACHE_NAME = 'weekly-schedule-cache-v1';
const urlsToCache = [
  '/',
  'index.html',
  'manifest.json',
  'icon.png'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE